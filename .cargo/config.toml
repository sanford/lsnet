# Link the C runtime statically, so lsnet.exe runs on machines without the
# Visual C++ Redistributable.
[target.'cfg(all(windows, target_env = "msvc"))']
rustflags = ["-C", "target-feature=+crt-static"]
